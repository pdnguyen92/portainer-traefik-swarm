# Dynamic configuration
# in configuration/certificates.yaml
tls:
  certificates:
    # first certificate
    - certFile: /etc/letsencrypt/live/pnt-life.com/fullchain.pem
      keyFile: /etc/letsencrypt/live/pnt-life.com/privkey.pem

    # second certificate
    #- certFile: /path/to/other.cert
    #  keyFile: /path/to/other.key

# and so on
